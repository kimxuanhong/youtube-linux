# youtube-linux

YouTube desktop wrapper for Linux built with Electron.

## Development

- Install dependencies: `npm install`
- Run app: `npm start`
- Build .deb package: `npm run dist`

## Notes

- Single-instance app
- Tray support (open, refresh, exit)
- Notification forwarding from website to native Linux notification
